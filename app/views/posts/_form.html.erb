<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group container">
  <div class="field mx-auto" style="width:400px">
    <%= f.label :기획날짜 %>
    <br>
    <%= f.date_select :date, class: "ff form-control text-center"%>
  </div>
  <br>
    <div class="field mx-auto" style="width:400px">
    <%= f.label :기획활동, class: "text-left" %>
    <%= f.text_field :title, class: "ff form-control text-center", placeholder: "겨울MT"  %>
  </div>
  <br>
      <div class="field mx-auto" style="width:400px">
    <%= f.label :인원수, class: "text-left" %>
    <%= f.number_field :member, class: "ff form-control text-center", placeholder: "4" %>
  </div>
  <br>
    <div class="field mx-auto" style="width:400px">
    <%= f.label :계좌, class: "text-left" %>
    <%= f.text_field :etc, class: "ff form-control text-center", placeholder: "하나 514-118-123456" %>
  </div>
  <br>
  </div>

  <div class="d-flex justify-content-md-center">
    <button class="btn btn-info">
      <%= link_to '뒤로', posts_path %>
      </button>
  
  &nbsp&nbsp  &nbsp    
  
    <div class="actions">
      <button type="submit" class="btn btn-info">
      제출
      </button>
  </div> 
  </div>   
    
<% end %>
</div>